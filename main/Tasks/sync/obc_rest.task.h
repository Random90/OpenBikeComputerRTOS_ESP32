#ifndef TASKS_SYNC_OBC_REST_H
#define TASKS_SYNC_OBC_REST_H

/**
 * @brief initialize ride end event listener, which will init and connect wifi, and send data to the obc server
 * */
void vRegisterServerSyncTask();

#endif